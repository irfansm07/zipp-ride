import React from 'react';
import ReactDOM from 'react-dom/client';
import ZippApp from '../ZippApp.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
    <React.StrictMode>
        <ZippApp />
    </React.StrictMode>
);
